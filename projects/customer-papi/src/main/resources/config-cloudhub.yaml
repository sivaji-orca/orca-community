http:
  host: "0.0.0.0"
  port: "8081"

backend:
  customer-api:
    host: "${customer.api.host}"
    port: "443"
    basePath: "/api"
    protocol: "HTTPS"
  sf-sapi:
    host: "${sf.sapi.host}"
    port: "443"
    basePath: "/api"
    protocol: "HTTPS"
